import { theme } from 'antd';

import type { CustomToken } from './types';

export const customToken: CustomToken = {
  canvasBackground: '#434343',
};

export const customTheme = {
  algorithm: [theme.darkAlgorithm, theme.compactAlgorithm],
};
